import Button from './Button/Button'
import { StyledButton, styles } from './Button/styled'
import {
  StyledButtonProps,
  ButtonProps,
} from './Button/types'

export {
  StyledButton,
  styles,
  StyledButtonProps,
  ButtonProps,
}
export default Button
