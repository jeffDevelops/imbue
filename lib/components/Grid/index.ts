import Grid from './Grid'
import GridArea from './GridArea'

import { GridProps, GridAreaProps } from './types'
import {
  styles,
  StyledGrid,
  StyledGridArea,
} from './styled'

export default Grid
export {
  GridArea,
  GridProps,
  GridAreaProps,
  styles,
  StyledGrid,
  StyledGridArea,
}
