import Checkbox from './Checkbox'
import { CheckboxProps, StyledCheckboxProps } from './types'
import { styles, CustomCheckbox } from './styled'

export default Checkbox
export {
  CheckboxProps,
  StyledCheckboxProps,
  styles,
  CustomCheckbox,
}
